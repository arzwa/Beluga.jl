language: julia

jobs:
  include:
    - stage: documentation
      julia: 1.3
      os: linux
      script:
        - julia --project=docs/ -e 'using Pkg; Pkg.add("https://github.com/arzwa/AdaptiveMCMC.jl"); 
                                    Pkg.instantiate(); Pkg.develop(PackageSpec(path=pwd()))'
        - julia --project=docs/ docs/make.jl
      after_success: skip
after_success: skip
